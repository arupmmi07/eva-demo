
  # Production-ready React app

  This is a code bundle for Production-ready React app. The original project is available at https://www.figma.com/design/ifOi6RwqKQaSggp8G7pcuz/Production-ready-React-app.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  