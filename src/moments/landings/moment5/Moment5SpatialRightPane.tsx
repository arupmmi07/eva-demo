import { ScheduleSpatialRightPane } from '../ScheduleSpatialRightPane';

/** Moment5 — daily schedule + KPIs (`public/moment5` reference). */
export function Moment5SpatialRightPane() {
  return <ScheduleSpatialRightPane dataName="Moment5SpatialRightPane" />;
}
